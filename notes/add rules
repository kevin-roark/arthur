arthur.add(media);

Frame frame = get(x, y, w, h); 
frame.add(media);
arthur.add(frame);

arthur.add immediately puts stuff into play

arthur.store puts things in global context, but invisible

arthur.store(media);

in loop:

  if (ms() > 5000) { // after 5 seconds
    arthur.media.activate() ?
  }
